grive.js
===

**WIP** - Only does uploads at the moment

```
Usage:
  grive [OPTIONS] <command> [ARGS]

Options:
  -c, --config [PATH]           Configuration file (Default is $HOME\.grive)
  -l, --limit [NUMBER]          Upload chunk limit (bytes) (Default is 524288000)
  -y, --consumerKey [STRING]    application consumer key (Default is 20399401188-n7or3f5551ahg6skvtdl6o4fbdrnhmf4.apps.googleusercontent.com)
  -s, --consumerSecret [STRING] application consumer secret (Default is 6M1rgY9AYzZDOzwtUWIVrxew)
  -v, --version                 Display the current version
  -h, --help                    Display help and usage details

Commands:
  init                   setup tokens
  upload                 upload a file
```
